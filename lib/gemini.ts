export async function generateWasteInsight(donorId: string): Promise<never> {
  throw new Error(`Not implemented: generateWasteInsight ${donorId}`);
}

export async function extractAllergens(ingredients: string): Promise<never> {
  throw new Error(`Not implemented: extractAllergens ${ingredients}`);
}
